class Pokemon {
  static pokemons = [];

  constructor(name, number, type, hp) {
    this.name = name;
    this.number = number;
    this.type = type;
    this.hp = hp;
  }
  static nameIsString(pokemon) {
      return typeof pokemon.name === "string" && name.length > 0;

  }
  static numberIsTypeNumber(pokemonNumber) {
      return typeof pokemon.number === "number"

  }

  static isValidType() {
    const elements = [];
  }
  static isValidHp(hp) {

  }
  static isValidPokemon(name, number, type, hp) {

  }
  assignType() {

  }
}

let pikachu = new Pokemon ("Pika" 3 "Animal")

